# Application version information - auto-generated during build
app.version=${project.version}
app.build.timestamp=${maven.build.timestamp}
app.name=${project.name}
app.description=${project.description}